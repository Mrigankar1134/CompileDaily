{
  "name": "javacareerprep-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for Compile Daily (Moushana's Java career-prep app). Holds the Neon Postgres and Gemini API credentials server-side; the app never sees them directly.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "seed": "node seed.js",
    "import-resources": "node import-resources.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
